# cpplint 配置文件 - 针对 cli 目录
# 设置项目根目录，防止向上查找父目录配置
set noparent

# 排除 argparse.hpp 文件
exclude_files=argparse\.hpp

# 过滤规则
filter=-whitespace/line_length,-build/include_order,-build/c++11

# 行长度限制
linelength=120

# 根目录设置
root=src

# 头文件扩展名
headers=hpp,hxx,h
